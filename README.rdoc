== O Condado

* Ruby version
2.2 with Rails 4.

* System dependencies
Until yet, nothing.

* Configuration
Until yet, nothing.

* Database creation
Currently, using SQLite 3. In future, we will use MongoDB.

* Database initialization
Just `rake db:migrate`.

* How to run the test suite
Until yet, nothing.

* Services (job queues, cache servers, search engines, etc.)
Until yet, nothing.

* Deployment instructions
Push to branch production, git hook will deploy it.